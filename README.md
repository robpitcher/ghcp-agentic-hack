# GitHub Copilot Agentic Hack

This public repository contains an approved workshop release exported from the private authoring repository. The release manifest and `release-provenance.json` identify the reviewed source commit.

Pull requests run type checking, tests, content validation, and a complete static build. Merges to `main` deploy the portal and Slidev decks to GitHub Pages.
